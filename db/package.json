{
  "name": "@mergenote/db",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "migrate": "node --import=dotenv/config dist/migrate.js",
    "migrate:dev": "tsx --import=dotenv/config src/migrate.ts"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "@types/pg": "^8.15.4",
    "typescript": "^5.8.3"
  }
}
